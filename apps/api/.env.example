# Paths are resolved from the repository root.
VYNEMA_DB_PATH=.local/vynema.sqlite

# Optional placeholders for later local-only auth work. Never commit real values.
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
SESSION_SECRET=
